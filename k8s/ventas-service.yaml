apiVersion: v1
kind: Service
metadata:
  name: ventas-service
  namespace: proyecto
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing

spec:
  type: LoadBalancer

  selector:
    app: ventas-service

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080